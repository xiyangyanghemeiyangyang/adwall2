import { Typography } from 'antd';

const { Title, Text } = Typography;

const OperatorDashboard = () => {
  return (
    <div>
      <Title level={4}>运营者首页</Title>
      <Text type="secondary">这里是运营者的空白首页内容区域。</Text>
    </div>
  );
};

export default OperatorDashboard;


